% set document borders
\usepackage[
	a4paper,
	top=20mm,
	left=18mm,
	right=18mm,
	bottom=20mm
]{geometry}

\usepackage[protrusion=true, expansion=true]{microtype}
\usepackage{paracol}

\usepackage{hyperref} % use urls
\usepackage{xcolor} % use colors
\usepackage{graphbox} % use graphics

% adds background watermark
\usepackage{draftwatermark}
\SetWatermarkText{\includegraphics{assets/watermark.eps}}
\SetWatermarkAngle{0}

\usepackage{ragged2e}

% set text lineheight
\usepackage{setspace}
\onehalfspacing

% set fonts
\usepackage{fontspec}
\setmainfont{Manrope}[
	Path = assets/fonts/,
	Extension = .ttf,
	UprightFont = *-Regular,
	ItalicFont = *-Italic,
	BoldFont = *-Bold,
	BoldItalicFont = *-BoldItalic
]
\newfontfamily{\optifont}{OPTIEinstein}[
	Path = assets/fonts/,
	Extension = .otf,
	UprightFont = *-Black,
	BoldFont = *-Black
]

% set variables
\newcommand{\accentColor}{\color[HTML]{7C6A60}}
\newcommand{\blackColor}{\color[HTML]{1D1B1A}}
\newcommand{\grayColor}{\color[HTML]{7A7C7F}}
\newcommand{\textColor}{\color[HTML]{3A3636}}

% set typography
\usepackage{titlesec}

\titleformat*{\section}{\fontsize{20pt}{20pt}\optifont\accentColor}
\titlespacing*{\section}{0pt}{2.5ex}{0.5ex}

\titleformat*{\subsection}{\fontsize{12pt}{12pt}\bfseries\blackColor}
\titlespacing*{\subsection}{0pt}{2ex}{0.2ex}

\titleformat*{\subsubsection}{\fontsize{11pt}{11pt}\bfseries\grayColor}
\titlespacing*{\subsubsection}{0pt}{0ex}{0.6ex}

% formats
\newcommand{\smallFormat}{\fontsize{9pt}{9pt}\bfseries\grayColor}
\newcommand{\urlFormat}[1]{\fontsize{9pt}{9pt}\bfseries\grayColor\underline{#1}}

% set color for normal text
\textColor

% set list style
\usepackage{enumitem}

\setlist[itemize]{%
	label=\textbullet,
	labelsep=0.5em,
	left=0.4em,
	itemsep=0.3ex,
	parsep=0pt,
}

% disable page numbering and set default text color
\pagenumbering{gobble}
